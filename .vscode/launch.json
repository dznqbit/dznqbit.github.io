{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "chrome",
          "request": "launch",
          "name": "Localhost",
          "url": "http://localhost:4000",
          "webRoot": "${workspaceFolder}",
          "preLaunchTask": "jekyll"
      }
  ]
}